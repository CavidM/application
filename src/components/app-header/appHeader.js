import React from 'react';
import Logo from 'components/logo/logo';

export default function AppHeader() {

    return (
        <React.Fragment>
            <Logo/>
        </React.Fragment>
    );
}